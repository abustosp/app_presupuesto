# Puerto expuesto por la API (host)
APP_PORT=8000

# URL de conexión a la base de datos. Por defecto usa SQLite dentro del contenedor.
DATABASE_URL=sqlite:///./data/app.db

# Orígenes habilitados para CORS (separados por coma). Usa "*" para todos.
ALLOWED_ORIGINS=*
